; path as seen from the compiler folder
(load "../content/lists")
(load "../content/set")
(load "../content/math")
(load "../content/required")

; main
(defun main ()
  (print "list functions:")
  (list_)
  (print "set functions:")
  (set)
  (print "math functions:")
  (math)
  (print "required functions:")
  (required)
)
